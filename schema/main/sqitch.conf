[core]
	engine = pg
	# plan_file = sqitch.plan
	# top_dir = .
# [engine "pg"]
	# target = db:pg:
	# registry = sqitch
	# client = psql
[deploy]
verify = true
[rebase]
verify = true
[target "dashboard_ads"]
uri = db:pg://localhost:5432/dashboard_ads
[engine "pg"]
target = dashboard_ads
